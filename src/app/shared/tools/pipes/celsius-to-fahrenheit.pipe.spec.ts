import { CelsiusToFharenheitPipe } from './celsius-to-fahrenheit.pipe';

describe('CelsiusToFahrenheitPipe', () => {
  it('create an instance', () => {
    const pipe = new CelsiusToFharenheitPipe();
    expect(pipe).toBeTruthy();
  });
});
